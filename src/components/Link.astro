---
interface Props {
  href: string;
  [key: string]: unknown;
}

const { href, class: className, ...rest } = Astro.props;
---

<style>
  .link {
    color: var(--color-link);
  }

  .link:hover {
    color: var(--color-link-hover);
  }
</style>

<a href={href} class:list={["link", className]} {...rest}><slot /></a>
